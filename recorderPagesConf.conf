# This config file used to config the page show in rqsco_stone.html.
hasShowScore = yes               #是否显示打分页面,当做bool使用,not case sensitive:true,yes,on,1 means true;false,no,off,0 means false.

#录音打分页面的设定,在一个table中的显示
[ PolicyHolder_Usr ]
#must have rowNumberPolicyHolder_Usr and itemCountPolicyHolder_Usr.
rowNumber = 6           #每行有多少列<td></td> 实际是乘以2的,因为都会有一个标签的.
itemCount = 13          #一共有多少项
#the setting order will decide show order.
#titelType must be in,otherwise the item will not be shown.
    [[ name ]]    #姓名
        titleText = '姓名'                      #头控件的内容
        titleType = label                       #头控件属性
        titleCss  = 'label label-normal'     #头控件的css
        dataType = input                        #数据控件的属性
        dataShowType = text                     #数据控件的控件属性
        dataCss  = 'input-mini'                 #数据控件的css
        dataName = "POLICYHOLDER_USR_NAME"          #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
        dataID  = "PolicyInformation1_txtName"  #数据控件的ID,js调用的时候需要
        placeholder  = "姓名"                     #没有数据时候的显示
        mustHave = yes                           #是否必须填写,如果必须填写,那么会加上一个*标记,在脚本中.
    [[ sex ]]    #性别
        titleText = '性别'                      #头控件的内容
        titleType = label                       #头控件属性
        titleCss  = 'label label-normal'     #头控件的css
        dataType = input                        #数据控件的属性
        dataShowType = radio                    #数据控件的控件属性
        dataName = "POLICYHOLDER_USR_SEX"           #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
        mustHave = no                           #是否必须填写,如果必须填写,那么会加上一个*标记,在脚本中.
        [[[radio]]]
            [[[[ radio1 ]]]]
            titleType = label                       #头控件属性
            titleCss  = 'radio inline'              #头控件的css
            inDataType = label                      #内部数据控件的属性
            inDataCss =  'label'                      #内部数据控件的css
            inDataText = '男'                         #数据控件的文本
            inDataValue = 0                         #数据控件的值
            inDataID  = "PolicyInformation_txtSexMan" #数据控件的ID,js调用的时候需要
            [[[[ radio2 ]]]]
            titleType = label                       #头控件属性
            titleCss  = 'radio inline'              #头控件的css
            inDataType = label                      #内部数据控件的属性
            inDataCss =  'label'                      #内部数据控件的css
            inDataText = '女'                         #数据控件的文本
            inDataValue = 1                         #数据控件的值
            inDataID  = "PolicyInformation_txtSexWoMan" #数据控件的ID,js调用的时候需要
    [[ birthday ]]    #出生日期
            titleText = '出生日期'                  #头控件的内容
            titleType = label                       #头控件属性
            titleCss  = 'label label-normal'     #头控件的css
            dataType = input                        #数据控件的属性
            dataShowType = textDate                     #数据控件的控件属性
            dataRealShowType = text                     #数据控件的真实控件属性
            onClick = "WdatePicker({el:'PolicyInformation1_txtBirthday',dateFmt:'yyyy-MM-dd',onpicked:pickedFuncPolicyInformation1_txtHolderOldYears})"                    #日期控件的点击事件
            dataCss  = 'Wdate input-small'                 #数据控件的css
            dataName = "POLICYHOLDER_USR_BIRTHDAY"          #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
            dataID  = "PolicyInformation1_txtBirthday"  #数据控件的ID,js调用的时候需要
            placeholder  = "出生日期"                     #没有数据时候的显示
            mustHave = yes                           #是否必须填写,如果必须填写,那么会加上一个*标记,在脚本中.
    [[ age ]]    #年龄
            titleText = '年龄'                      #头控件的内容
            titleType = label                       #头控件属性
            titleCss  = 'label label-normal'     #头控件的css
            dataType = input                        #数据控件的属性
            dataShowType = text                     #数据控件的控件属性
            dataCss  = 'input-mini'                 #数据控件的css
            dataName = "POLICYHOLDER_USR_AGE"          #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
            dataID  = "PolicyInformation1_txtHolderOldYears"  #数据控件的ID,js调用的时候需要
            placeholder  = "年龄"                     #没有数据时候的显示
            mustHave = no                           #是否必须填写,如果必须填写,那么会加上一个*标记,在脚本中.
    [[ profession ]]    #职业
            titleText = '职业'                      #头控件的内容
            titleType = label                       #头控件属性
            titleCss  = 'label label-normal'                     #头控件的css
            defaultValue = '职员'                                 #默认值
            dataType = select                        #数据控件的属性
            dataShowType = selectConf                    #数据控件的控件属性,selectConf,从其它配置文件中读取值
            dataSourceId = 'OCCUPATION'                       #数据源的编号
            dataTitleType = OPTION                       #头控件属性
            dataName = "POLICYHOLDER_USR_PROFESSION"           #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
            dataID  = "PolicyInformation1_ddlProfessional"  #数据控件的ID,js调用的时候需要
            dataCss = 'input-small'                #数据控件的css
    [[ phone ]]    #联系电话
                titleText = '联系电话'                      #头控件的内容
                titleType = label                       #头控件属性
                titleCss  = 'label label-normal'     #头控件的css
                dataType = input                        #数据控件的属性
                dataShowType = text                     #数据控件的控件属性
                dataCss  = 'input-medium'                 #数据控件的css
                dataName = "POLICYHOLDER_USR_PHONE"          #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
                dataID  = "PolicyInformation1_txtHolderPhoneNumber"  #数据控件的ID,js调用的时候需要
                placeholder  = "联系电话"                     #没有数据时候的显示
                mustHave = yes                           #是否必须填写,如果必须填写,那么会加上一个*标记,在脚本中.
    [[ idcardtype ]]    #证件类型
                titleText = '证件类型'                      #头控件的内容
                titleType = label                       #头控件属性
                titleCss  = 'label label-normal'     #头控件的css
                defaultValue = '身份证'                                 #默认值
                dataType = select                        #数据控件的属性
                dataShowType = selectConf                    #数据控件的控件属性,selectConf,从其它配置文件中读取值
                dataSourceId = 'CARDTYPE'                       #数据源的编号
                dataTitleType = OPTION                       #头控件属性
                dataName = "POLICYHOLDER_USR_IDCARDTYPE"           #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
                dataID  = "PolicyInformation1_sHolderCardType"  #数据控件的ID,js调用的时候需要
                dataCss = 'input-small'                #数据控件的css
    [[ idcardno ]]    #证件号码
            titleText = '证件号码'                      #头控件的内容
            titleType = label                       #头控件属性
			titleCss  = 'label label-normal'     #头控件的css
            dataType = input                        #数据控件的属性
            dataShowType = text                     #数据控件的控件属性
            dataCss  = 'input-medium'                 #数据控件的css
            dataName = "POLICYHOLDER_USR_IDCARDNO"          #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
            dataID  = "PolicyInformation1_txtHolderCardNumber"  #数据控件的ID,js调用的时候需要
            placeholder  = "证件号码"                     #没有数据时候的显示
			mustHave = no                           #是否必须填写,如果必须填写,那么会加上一个*标记,在脚本中.
    [[ height ]]    #身高
            titleText = '身高'                      #头控件的内容
            titleType = label                       #头控件属性
			titleCss  = 'label label-normal'     #头控件的css
            dataType = input                        #数据控件的属性
            dataShowType = text                     #数据控件的控件属性
            dataCss  = 'input-mini'                 #数据控件的css
            dataName = "POLICYHOLDER_USR_HEIGHT"          #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
            dataID  = "PolicyInformation1_txtHolderUsrHeight"  #数据控件的ID,js调用的时候需要
            placeholder  = "身高"                     #没有数据时候的显示
    [[ weight ]]    #体重
                titleText = '体重'                      #头控件的内容
                titleType = label                       #头控件属性
				titleCss  = 'label label-normal'     #头控件的css
                dataType = input                        #数据控件的属性
                dataShowType = text                     #数据控件的控件属性
                dataCss  = 'input-mini'                 #数据控件的css
                dataName = "POLICYHOLDER_USR_WEIGHT"          #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
                dataID  = "PolicyInformation1_txtHolderUsrWeight"  #数据控件的ID,js调用的时候需要
                placeholder  = "体重"                     #没有数据时候的显示
    [[ briefAddress ]]    #省市区
            titleText = '省市区'                      #头控件的内容
            titleType = label                       #头控件属性
			titleCss  = 'label label-normal'     #头控件的css
            dataType = select                        #数据控件的属性
            dataShowType = selectBriefAddress                    #数据控件的控件属性
            scriptFile = 'changevalCityIns(document.getElementById("sheng"),0,1);'
            [[[select]]]
                [[[[ select1 ]]]] #省
                titleType = select                       #头控件属性
                inDataName = "sheng"           #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
                inDataID  = "sheng"  #数据控件的ID,js调用的时候需要
                inDataCss = 'input-small'                #数据控件的css
                onchange = 'changevalCityIns(document.getElementById('shi'),this.value);'
                onSetValueDataID = "shi"                #需要改变的控件ID.
                [[[[ select2 ]]]] #市
                titleType = select                       #头控件属性
                inDataName = "shi"           #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
                inDataID  = "shi"  #数据控件的ID,js调用的时候需要
                inDataCss = 'input-small'                #数据控件的css
                onchange = 'changevalCityIns(document.getElementById('xian'),this.value);'
                onSetValueDataID = "xian"                #需要改变的控件ID.
                [[[[ select3 ]]]] #县
                titleType = select                       #头控件属性
                inDataName = "xian"           #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
                inDataID  = "xian"  #数据控件的ID,js调用的时候需要
                inDataCss = 'input-small'                #数据控件的css
    [[ detailAddress ]]    #详细地址
                titleText = '详细地址'                      #头控件的内容
                titleType = label                       #头控件属性
				titleCss  = 'label label-normal'     #头控件的css
                dataType = input                        #数据控件的属性
                dataShowType = text                     #数据控件的控件属性
                dataCss  = 'input-medium'                 #数据控件的css
                dataName = "POLICYHOLDER_USR_ADDRESS"          #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
                dataID  = "PolicyInformation1_txtHolderDetailAddress"  #数据控件的ID,js调用的时候需要
                placeholder  = "详细地址"                     #没有数据时候的显示
				mustHave = no                           #是否必须填写,如果必须填写,那么会加上一个*标记,在脚本中.
    [[ postcode ]]    #邮编
                titleText = '邮编'                      #头控件的内容
                titleType = label                       #头控件属性
				titleCss  = 'label label-normal'     #头控件的css
                dataType = input                        #数据控件的属性
                dataShowType = text                     #数据控件的控件属性
                dataCss  = 'input-small'                 #数据控件的css
                dataName = "POLICYHOLDER_USR_ZIP"          #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
                dataID  = "PolicyInformation1_txtHolderZip"  #数据控件的ID,js调用的时候需要
                placeholder  = "邮编"                     #没有数据时候的显示
				mustHave = no                           #是否必须填写,如果必须填写,那么会加上一个*标记,在脚本中.

        dataShowType = radio                    #数据控件的控件属性
        dataName = "INSURANT_USR_SEX"           #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
		mustHave = no                           #是否必须填写,如果必须填写,那么会加上一个*标记,在脚本中.
        [[[radio]]]
            [[[[ radio1 ]]]]
            titleType = label                       #头控件属性
            titleCss  = 'radio inline'              #头控件的css
            inDataType = label                      #内部数据控件的属性
            inDataCss =  'label'                      #内部数据控件的css
            inDataText = '男'                         #数据控件的文本
            inDataValue = 0                         #数据控件的值
            inDataID  = "BePolicyHolder_rbtBeHolderSexMan" #数据控件的ID,js调用的时候需要
            [[[[ radio2 ]]]]
            titleType = label                       #头控件属性
            titleCss  = 'radio inline'              #头控件的css
            inDataType = label                      #内部数据控件的属性
            inDataCss =  'label'                      #内部数据控件的css
            inDataText = '女'                         #数据控件的文本
            inDataValue = 1                         #数据控件的值
            inDataID  = "BePolicyHolder_rbtBeHolderSexWoman" #数据控件的ID,js调用的时候需要
    [[ birthday ]]    #出生日期
            titleText = '出生日期'                  #头控件的内容
            titleType = label                       #头控件属性
			titleCss  = 'label label-normal'     #头控件的css
            dataType = input                        #数据控件的属性
            dataShowType = textDate                     #数据控件的控件属性
            dataRealShowType = text                     #数据控件的真实控件属性
            onClick = "WdatePicker({el:'BePolicyHolder_txtBeHolderBirthday',dateFmt:'yyyy-MM-dd',onpicked:pickedFuncBePolicyHolder_txtBeHolderOldYears})"                    #日期控件的点击事件
            dataCss  = 'Wdate input-small'                 #数据控件的css
            dataName = "INSURANT_USR_BIRTHDAY"          #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
            dataID  = "BePolicyHolder_txtBeHolderBirthday"  #数据控件的ID,js调用的时候需要
            placeholder  = "出生日期"                     #没有数据时候的显示
			mustHave = yes                           #是否必须填写,如果必须填写,那么会加上一个*标记,在脚本中.
    [[ age ]]    #年龄
            titleText = '年龄'                      #头控件的内容
            titleType = label                       #头控件属性
			titleCss  = 'label label-normal'     #头控件的css
            dataType = input                        #数据控件的属性
            dataShowType = text                     #数据控件的控件属性
            dataCss  = 'input-mini'                 #数据控件的css
            dataName = "INSURANT_USR_AGE"          #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
            dataID  = "BePolicyHolder_txtBeHolderOldYears"  #数据控件的ID,js调用的时候需要
            placeholder  = "年龄"                     #没有数据时候的显示
			mustHave = no                           #是否必须填写,如果必须填写,那么会加上一个*标记,在脚本中.
    [[ profession ]]    #职业
            titleText = '职业'                      #头控件的内容
            titleType = label                       #头控件属性
			titleCss  = 'label label-normal'     #头控件的css
            dataType = select                        #数据控件的属性
            dataName = "INSURANT_USR_PROFESSION"           #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
            dataID  = "BePolicyHolder_ddlBeHolderProfessional"  #数据控件的ID,js调用的时候需要
            dataCss = 'input-small'                #数据控件的css
            dataShowType = selectConf                    #数据控件的控件属性,selectConf,从其它配置文件中读取值
            dataSourceId = 'OCCUPATION'                       #数据源的编号
            dataTitleType = OPTION                       #头控件属性
			defaultValue = '职员'                                 #默认值
    [[ phone ]]    #联系电话
                titleText = '联系电话'                      #头控件的内容
                titleType = label                       #头控件属性
				titleCss  = 'label label-normal'     #头控件的css
                dataType = input                        #数据控件的属性
                dataShowType = text                     #数据控件的控件属性
                dataCss  = 'input-medium'                 #数据控件的css
                dataName = "INSURANT_USR_PHONE"          #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
                dataID  = "BePolicyHolder_txtBeHolderPhoneNumber"  #数据控件的ID,js调用的时候需要
                placeholder  = "联系电话"                     #没有数据时候的显示
				mustHave = yes                           #是否必须填写,如果必须填写,那么会加上一个*标记,在脚本中.
    [[ idcardtype ]]    #证件类型
                titleText = '证件类型'                      #头控件的内容
                titleType = label                       #头控件属性
				titleCss  = 'label label-normal'     #头控件的css
                defaultValue = '身份证'                                 #默认值
                dataType = select                        #数据控件的属性
                dataShowType = selectConf                    #数据控件的控件属性,selectConf,从其它配置文件中读取值
                dataSourceId = 'CARDTYPE'                       #数据源的编号
                dataTitleType = OPTION                       #头控件属性
                dataName = "INSURANT_USR_IDCARDTYPE"           #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
                dataID  = "BePolicyHolder_selectBeHolderCardType"  #数据控件的ID,js调用的时候需要
                dataCss = 'input-small'                #数据控件的css
    [[ idcardno ]]    #证件号码
            titleText = '证件号码'                      #头控件的内容
            titleType = label                       #头控件属性
			titleCss  = 'label label-normal'     #头控件的css
            dataType = input                        #数据控件的属性
            dataShowType = text                     #数据控件的控件属性
            dataCss  = 'input-medium'                 #数据控件的css
            dataName = "INSURANT_USR_IDCARDNO"          #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
            dataID  = "BePolicyHolder_txtBeHolderCardNumber"  #数据控件的ID,js调用的时候需要
            placeholder  = "证件号码"                     #没有数据时候的显示
			mustHave = no                           #是否必须填写,如果必须填写,那么会加上一个*标记,在脚本中.
    [[ height ]]    #身高
            titleText = '身高'                      #头控件的内容
            titleType = label                       #头控件属性
			titleCss  = 'label label-normal'     #头控件的css
            dataType = input                        #数据控件的属性
            dataShowType = text                     #数据控件的控件属性
            dataCss  = 'input-mini'                 #数据控件的css
            dataName = "INSURANT_USR_HEIGHT"          #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
            dataID  = "PolicyInformation1_txtBeHolderUsrHeight"  #数据控件的ID,js调用的时候需要
            placeholder  = "身高"                     #没有数据时候的显示
    [[ weight ]]    #体重
                titleText = '体重'                      #头控件的内容
                titleType = label                       #头控件属性
				titleCss  = 'label label-normal'     #头控件的css
                dataType = input                        #数据控件的属性
                dataShowType = text                     #数据控件的控件属性
                dataCss  = 'input-mini'                 #数据控件的css
                dataName = "INSURANT_USR_WEIGHT"          #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
                dataID  = "PolicyInformation1_txtBeHolderUsrWeight"  #数据控件的ID,js调用的时候需要
                placeholder  = "体重"                     #没有数据时候的显示
    [[ briefAddress ]]    #省市区
            titleText = '省市区'                      #头控件的内容
            titleType = label                       #头控件属性
			titleCss  = 'label label-normal'     #头控件的css
            dataType = select                        #数据控件的属性
            dataShowType = selectBriefAddress                    #数据控件的控件属性
            scriptFile = 'changevalCityPls(document.getElementById("idINSURANT_USR_sheng"),0,1);'
            [[[select]]]
                [[[[ select1 ]]]] #省
                titleType = select                       #头控件属性
                inDataName = "INSURANT_USR_sheng"           #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
                inDataID  = "idINSURANT_USR_sheng"  #数据控件的ID,js调用的时候需要
                inDataCss = 'input-small'                #数据控件的css
                onchange = 'changevalCityPls(document.getElementById('idINSURANT_USR_shi'),this.value);'
                onSetValueDataID = "idINSURANT_USR_shi"                #需要改变的控件ID.
                [[[[ select2 ]]]] #市
                titleType = select                       #头控件属性
                inDataName = "INSURANT_USR_shi"           #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
                inDataID  = "idINSURANT_USR_shi"  #数据控件的ID,js调用的时候需要
                inDataCss = 'input-small'                #数据控件的css
                onchange = 'changevalCityPls(document.getElementById('idINSURANT_USR_xian'),this.value);'
                onSetValueDataID = "idINSURANT_USR_xian"                #需要改变的控件ID.
                [[[[ select3 ]]]] #县
                titleType = select                       #头控件属性
                inDataName = "INSURANT_USR_xian"           #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
                inDataID  = "idINSURANT_USR_xian"  #数据控件的ID,js调用的时候需要
                inDataCss = 'input-small'                #数据控件的css
    [[ detailAddress ]]    #详细地址
                titleText = '详细地址'                      #头控件的内容
                titleType = label                       #头控件属性
				titleCss  = 'label label-normal'     #头控件的css
                dataType = input                        #数据控件的属性
                dataShowType = text                     #数据控件的控件属性
                dataCss  = 'input-medium'                 #数据控件的css
                dataName = "INSURANT_USR_ADDRESS"          #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
                dataID  = "idINSURANT_USR_ADDRESS"  #数据控件的ID,js调用的时候需要
                placeholder  = "详细地址"                     #没有数据时候的显示
				mustHave = no                           #是否必须填写,如果必须填写,那么会加上一个*标记,在脚本中.
    [[ postcode ]]    #邮编
                titleText = '邮编'                      #头控件的内容
                titleType = label                       #头控件属性
				titleCss  = 'label label-normal'     #头控件的css
                dataType = input                        #数据控件的属性
                dataShowType = text                     #数据控件的控件属性
                dataCss  = 'input-small'                 #数据控件的css
                dataName = "INSURANT_USR_ZIP"          #数据控件的名字,在form回传的时候作为form的参数,不需要和传给REST服务的JSON对应，这个只是页面的控件名称，不重复即可,JSON是在代码中对应的.
                dataID  = "PolicyInformation1_txtBeHolderZip"  #数据控件的ID,js调用的时候需要
                placeholder  = "邮编"                     #没有数据时候的显示
				mustHave = no                           #是否必须填写,如果必须填写,那么会加上一个*标记,在脚本中.

